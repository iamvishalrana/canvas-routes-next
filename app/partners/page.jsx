import PartnerContent from '../../components/PartnerContent'

export const metadata = {
  title: { absolute: 'Partners | Canvas Routes' },
  robots: { index: false, follow: false },
}

export default function PartnersPage() {
  return <PartnerContent />
}
